import sys
for lines in sys.stdin:
    print(len(lines))
# print command line lines

for lines in sys.stdin:
    print(lines[0])
# takes from command line and prints the first character

import math


def x_to_the_y(x,y):
    return x ** y

